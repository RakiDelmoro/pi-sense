# Dependencies
node_modules

# Environment — secrets and local overrides
.env
.env.local

# Build output
dist/

# Database storage
.influxdb-data/

# MQTT credentials — baked into Docker image at build time, never on host

# Pi agent internals — not part of the app
.context/

# Sensors
sensors/

# Project docs
docs/

# Stray images in project root (screenshots, debug captures)
/*.jpg
/*.gif
/*.webp

# OS/editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~