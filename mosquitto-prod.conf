# Mosquitto MQTT broker configuration for Pi Sense
# Requires username/password (production mode)

listener 1883
allow_anonymous false
password_file /mosquitto/config/auth/credentials

# Debug logging — shows connections, subscriptions, and published messages
log_type all
log_dest stdout
connection_messages true
